language: python
python:
  - "3.6"

install:
  - pip install --upgrade pip
  - pip install -U -r requirements.txt

script:
  - git submodule init
  - git submodule update
  - pip install -U bpl/src/requirements.txt
  - python bpl/src/setup.py develop test
  - pytest
